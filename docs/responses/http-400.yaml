description: Bad Request. It means request does not pass validation.
content:
  application/json:
    schema:
      $ref: '../_index.yaml#/components/schemas/Error'
    example:
      error: 'request.body should have required property "field"'