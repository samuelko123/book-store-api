description: Unauthorized. It means you are not logged in.
headers:
  WWW_Authenticate:
    schema:
      type: string
content:
  application/json:
    schema:
      $ref: '../_index.yaml#/components/schemas/Error'
    example:
      error: 'Login failed. Please try again.'